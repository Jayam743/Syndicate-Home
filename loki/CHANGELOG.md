# Loki Defect CHANGELOG

> GENERATED by scripts/loki-changelog.sh — do not hand-edit.
> A regenerable VIEW over loki/logs/*.md: every `[applied]` finding that
> carries a `Pinned-by` ref. Rerun the script after any log status change;
> it rewrites this file from scratch (an applied finding that regresses and
> loses its Pinned-by simply drops out).

| Defect | Fix | Pinned-by | When |
|--------|-----|-----------|------|
| B2: hooks/godspeed.sh substring matchers false-positive on prose (Stop hook fired on 'deployable') | FIXED — godspeed.sh now scans tool_use command payloads for ABSOLUTE_GATES instead of matching prose keywords, so status text like 'deployable' no longer trips the gate (#15). NOTE: this fixes Syndicate's OWN godspeed.sh matcher only; BJ's stop-action-bias-detector.sh prose false-positives remain a separate carried issue (upstream to BJ). | PR #27 (50ae745) | 2026-08-19T17:38:22-04:00 |
| Issue #8 computed-fan design targets the wrong variable: it computes fan-safety from campaign.js's dependency graph, but that graph only encodes ordering-deps (wave membership), NOT file-set disjointness. Two same-wave items with no declared dep can both edit the same file. Worse: campaign.js executeItem does git checkout -b in a SHARED working tree (no worktree isolation) — so parallel() fans agents into one index/HEAD => corruption regardless of file disjointness. Real fan-safety variable = worktree isolation + disjoint file sets, neither of which the dep graph carries. | AMEND: recommend-only + deterministic fail-closed fan-eligibility predicate (width>1 AND declared disjoint file sets AND isolated worktrees AND mechanical); replace per-wave Loki LLM spawn with a code gate + single autoFan:false arg. SHIPPED as PR #33: pure predicate scripts/lib/fan-eligibility.js (89 unit tests, glob-aware disjointness), paths[]+mechanical data model on campaign items, predicate INLINED into campaign.js (validate.sh drift-guard enforces sync), LLM wave.dispatch demoted to informational. OUTCOME = SAFE DEGRADE-TO-SERIALIZE: fanOut = eligible && worktreeIsolationAvailable; the capability probe resolves FALSE (harness doesn't advertise worktreeIsolation) so fan is DORMANT — every wave serializes + logs its eligibility+reason. Athena CONFIRMED parallel() unreachable without proven isolation. ACTIVATION tracked in follow-up issue #32 (verify live isolation → flip gate). RE the toolkit.md:144 commutativity/flight_partition sub-note: RE-EVALUATED → invalid, no consumption/no code change needed (see the [wontfix] entry dated 2026-08-27). | PR #33 (be37c46); activation follow-up = issue #32 | 2026-08-27T11:17:55-04:00 |
| cost-report.sh under-counted nested-subagent cost — find -maxdepth 1 scanned only sibling transcripts, so nested <session-id>/subagents/agent-*.jsonl spend counted as ZERO (Opus-4.8 delegation metric read misleadingly high) | FIXED — cost-report.sh now session-scopes discovery of nested <session-id>/subagents/agent-*.jsonl (honest subagent cost); regression test scripts/ci/test-cost-report.sh (10 assertions) wired into validate.sh. Shipped with the warn-only per-pipeline cost governor (mechanism D, folded into standard-pipeline.js Record/Ledger stage; $4 soft default, never halts). | PR #29 (efe7010) | 2026-08-27T15:45:00-04:00 |
| post-tool-test-sentinel.sh resolved repo context from the hook's ambient cwd (git branch --show-current) instead of the payload, so the test sentinel could be written/keyed against the wrong repo when cwd drifted | FIXED — sentinel is now worktree-keyed at ~/.syndicate/sentinels/<sha1(repo-toplevel)>, resolved from hook payload .cwd; adds GC of stale orphans + fail-closed gate; godspeed keyed the same way. Contract documented in the gate + AXIOMS Axiom 7. Test scripts/ci/test-sentinel.sh (19 assertions). | PR #31 (0a8746e) | 2026-08-27T15:45:30-04:00 |
| CRITICAL false-green: the #30 sentinel WRITER honoring git -C would let a test run in one repo mint a green sentinel for another (a false test-pass vector unlocking Hermes push) — caught by Athena in review before ship | FIXED before ship — writer keys the sentinel by .cwd ONLY; gate keeps git -C precedence for lookup. Athena's CRITICAL was closed by a dedicated false-green regression assertion in scripts/ci/test-sentinel.sh. | PR #31 (0a8746e) | 2026-08-27T15:46:00-04:00 |
| Main-loop silently skipped Scribe(0.5)/recall(1.5) on substantial requests (~12 findings/month) | Added UserPromptSubmit preflight hook (user-prompt-preflight.sh) injecting a reminder on substantial prompts; doctrine+toolkit updated; test-preflight.sh 9 assertions | PR #45 (d28360c) | 2026-08-27T20:46:24-04:00 |
| Per-spawn model override 400'd on invalid id (sonnet-4-5[1m]); mechanical spawns inherited Opus 4.8 (no cost savings) | config/models.md documents Agent-tool ENUM override (haiku/sonnet, harness-mapped, never raw id) + doctrine Cost Directive rule; workflow side uses BAND (validate.sh-enforced) | PR #45 (d28360c) | 2026-08-27T20:46:24-04:00 |
| cost-report.sh over-estimated ~2x (on-demand list, phantom [1m] premium, main-transcript-only subagent under-count) | Removed phantom [1m] premium; added BEDROCK_COST_FACTOR (0.53 est-actual); session-scoped subagent discovery; cwd-toplevel resolution | PR #37/#29/#40 (4bc6f6f) | 2026-08-27T20:46:24-04:00 |
